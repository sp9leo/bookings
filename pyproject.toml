[project]
name = "bookings"
authors = [
    { name = "OS\u0160", email = "info@osaz.si" }
]
description = "Booking system for persons, assets and rooms"
requires-python = ">=3.11"
readme = "README.md"
dynamic = ["version"]
dependencies = [
    # "frappe~=16.0.0" # Installed and managed by bench.
]

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
